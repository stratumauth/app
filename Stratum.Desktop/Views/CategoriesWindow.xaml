<Window x:Class="Stratum.Desktop.Views.CategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Manage Categories"
        Height="400" Width="350"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Categories" Style="{StaticResource HeadlineStyle}" Margin="0,0,0,16"/>

        <!-- Category List -->
        <ListBox x:Name="CategoriesListBox" Grid.Row="1"
                 SelectionMode="Single"
                 BorderThickness="1"
                 BorderBrush="{StaticResource DividerBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="8,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Edit" Margin="8,0"
                                Style="{StaticResource IconButtonStyle}"
                                Width="Auto" Height="Auto" Padding="8,4"
                                Click="EditCategoryButton_Click"/>
                        <Button Grid.Column="2" Content="Delete"
                                Style="{StaticResource IconButtonStyle}"
                                Width="Auto" Height="Auto" Padding="8,4"
                                Foreground="Red"
                                Click="DeleteCategoryButton_Click"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Add Category -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewCategoryTextBox" Grid.Column="0"
                     Style="{StaticResource SearchTextBoxStyle}"
                     MaxLength="32"
                     Margin="0,0,8,0"/>
            <Button Grid.Column="1" Content="Add"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="AddCategoryButton_Click"/>
        </Grid>
    </Grid>
</Window>
