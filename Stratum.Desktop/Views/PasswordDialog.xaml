<Window x:Class="Stratum.Desktop.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Password"
        SizeToContent="Height"
        Width="350"
        MinHeight="200"
        MaxHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" Text="Enter password:"
                   Style="{StaticResource TitleStyle}" Margin="0,0,0,16"
                   TextWrapping="Wrap"/>

        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Padding="12,8" FontSize="14"/>

        <StackPanel x:Name="ConfirmPanel" Grid.Row="2" Margin="0,12,0,0" Visibility="Collapsed">
            <TextBlock Text="Confirm password:" Margin="0,0,0,8"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Padding="12,8" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource IconButtonStyle}"
                    Width="Auto" Height="Auto" Padding="16,8"
                    Click="CancelButton_Click" Margin="0,0,8,0"/>
            <Button Content="OK"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
